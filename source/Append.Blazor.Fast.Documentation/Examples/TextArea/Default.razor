<ExampleHeader Component="typeof(TextArea)" Example="@GetType()" />

<EditForm Model="model" OnValidSubmit="HandleValidSubmit">
    <TextArea @bind-Value="model.Text"></TextArea>
</EditForm>
<p>The current value is: @model.Text</p>

@code{
    private EditModel model = new EditModel();
    public class EditModel
    {
        public string Text { get; set; } = "Some default text...";
    }
    private void HandleValidSubmit()
    {
        Console.WriteLine($"Value of the model is: {model.Text}");
    }
}